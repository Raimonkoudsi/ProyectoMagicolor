<Window x:Class="ProyectoMagicolor.Vistas.ProveedorFrm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProyectoMagicolor.Vistas"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Variedades Magicolor" WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize" Height="615" Width="470" Loaded="Window_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="#3194F7">
            <Image Width="40" Margin="30 0 0 0" VerticalAlignment="Center" HorizontalAlignment="Left" Source="..\Imagenes\icon-bag.png"/>
            <TextBlock Name="txtTitulo"
                       Text="Insertar Proveedor" 
                       HorizontalAlignment="Center"
                       Foreground="White"
                       FontWeight="SemiBold"
                       FontSize="28"
                       Margin="0 10"
                        />
        </Grid>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Visible" CanContentScroll="True" >
            <StackPanel Width="380" Margin="17 0 0 0">
                <Grid Margin="0 20 0 15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid Margin="0 0">
                        <TextBox x:Name="txtRazonSocial" MaxLength="100" FontSize="12"
                            materialDesign:HintAssist.Hint="Razón Social"
                            Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </Grid>
                    <Grid Grid.Column="1" Margin="10 0 0 0">
                        <ComboBox x:Name="CbSectorComercial" FontSize="12"
                            materialDesign:HintAssist.Hint="Sector Comercial"
                            Style="{StaticResource MaterialDesignOutlinedComboBox}">
                        </ComboBox>
                    </Grid>
                </Grid>
                <Grid Margin="0 15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid>
                        <ComboBox x:Name="CbTipoDocumento" FontSize="12"
                            materialDesign:HintAssist.Hint="Tipo"
                            Style="{StaticResource MaterialDesignOutlinedComboBox}">
                            <ComboBoxItem>V</ComboBoxItem>
                            <ComboBoxItem>E</ComboBoxItem>
                            <ComboBoxItem>J</ComboBoxItem>
                            <ComboBoxItem>G</ComboBoxItem>
                        </ComboBox>
                    </Grid>
                    <Grid Margin="10 0 0 0" Grid.Column="1">
                        <TextBox x:Name="txtDocumento" MaxLength="50" FontSize="12"
                            materialDesign:HintAssist.Hint="Número Documento"
                            Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </Grid>                
                </Grid>
                <Grid Margin="0 15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid Margin="0 0 10 0" Grid.Column="0">
                        <TextBox x:Name="txtTelefono" MaxLength="14" FontSize="12"
                            materialDesign:HintAssist.Hint="Teléfono"
                            Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </Grid>
                    <Grid Margin="0 0 0 0" Grid.Column="1">
                        <TextBox x:Name="txtEmail" MaxLength="50" FontSize="12"
                            materialDesign:HintAssist.Hint="Correo Electrónico (Opcional)"
                            Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </Grid>
                </Grid>
                <TextBox Margin="0 15" x:Name="txtUrl" MaxLength="100" FontSize="12"
                            materialDesign:HintAssist.Hint="Página Web (Opcional)"
                            Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                <Grid Margin="0 15">
                    <TextBox x:Name="txtDireccion" FontSize="12" Text="" 
                             Height="80" TextWrapping="Wrap"
                             AcceptsReturn="True"
                             MaxLength="100"
                             HorizontalScrollBarVisibility="Disabled"
                             VerticalScrollBarVisibility="Auto"
                             materialDesign:HintAssist.Hint="Descripción (Opcional)"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                </Grid>
                <Button Name="btnEnviar" Width="300" Background="#3194F7" Content="Enviar" Margin="0 30 0 0" Click="Button_Click"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
